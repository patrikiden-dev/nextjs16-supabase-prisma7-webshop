export type Product = {
    id: number
    title: string
    slug: string
    category: string
    brand: string
    price: number
    stock: number
    description: string
    image?: string
    thumbnail?: string
    added: string
    sku: string
}